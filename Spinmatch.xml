<?xml version="1.0"?>
<Container version="2">
  <Name>Spinmatch</Name>
  <Repository>ghcr.io/yoshiofthewire/spinmatch:latest</Repository>
  <Registry>https://github.com/Yoshiofthewire/Spinmatch/pkgs/container/spinmatch</Registry>
  <Network>bridge</Network>
  <MyIP></MyIP>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/Yoshiofthewire/Spinmatch/issues</Support>
  <Project>https://github.com/Yoshiofthewire/Spinmatch</Project>
  <Overview>Search MusicBrainz for an artist, album, or song, browse album art and tracklists, and get a YouTube link for a track, verified by cross-checking the video's duration against the MusicBrainz-recorded track length. Spinmatch only finds and verifies YouTube links; it does not download or rip audio itself. It can also organize a local folder of unsorted audio files into your music library, tagging tracks either automatically via acoustic fingerprinting (AcoustID/Chromaprint) or by manual MusicBrainz search.&#13;&#10;&#13;&#10;Requires a free MusicBrainz-compatible contact email (set below). The optional local library ingest feature needs Ingest Directory and Music Directory; a free AcoustID API key from https://acoustid.org/new-application additionally enables automatic fingerprint identification; without one, files are matched by their existing tags instead.</Overview>
  <Category>MediaApp:Music Tools:</Category>
  <WebUI>http://[IP]:[PORT:3000]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/Yoshiofthewire/Spinmatch/main/unraid-template.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/Yoshiofthewire/Spinmatch/main/client/public/favicon.svg</Icon>
  <ExtraParams></ExtraParams>
  <PostArgs></PostArgs>
  <CPUset></CPUset>
  <DateInstalled></DateInstalled>
  <DonateText></DonateText>
  <DonateLink></DonateLink>
  <Requires></Requires>
  <Config Name="WebUI Port" Target="3000" Default="3000" Mode="tcp" Description="Port the web UI listens on" Type="Port" Display="always" Required="true" Mask="false">3000</Config>
  <Config Name="PUID" Target="PUID" Default="99" Mode="" Description="User id the server runs as. Unraid's shares are owned by nobody:users (99:100), which is the default here — leave it alone unless you know your shares are owned by something else. It must be a uid that can write the Library DB Directory below, and the Music/Ingest directories if you use them. Upgrading from a version that ran as root? The container fixes up the DB directory itself on start; your music share is left alone." Type="Variable" Display="advanced" Required="false" Mask="false">99</Config>
  <Config Name="PGID" Target="PGID" Default="100" Mode="" Description="Group id the server runs as. Pairs with PUID above; 100 is Unraid's `users` group." Type="Variable" Display="advanced" Required="false" Mask="false">100</Config>
  <Config Name="Ingest Directory" Target="/data/ingest" Default="/mnt/user/appdata/spinmatch/ingest" Mode="rw" Description="Drop-folder for unsorted audio (loose files or whole album folders) to be identified and filed into Music Directory. Only used if MB Contact Email, Music Directory, and this path are all set. AcoustID API Key is optional on top of that — without it, files are matched by the tags they already carry, and anything that can't be confirmed is listed for manual review." Type="Path" Display="always" Required="false" Mask="false">/mnt/user/appdata/spinmatch/ingest</Config>
  <Config Name="Music Directory" Target="/data/music" Default="/mnt/user/Music" Mode="rw" Description="Your organized music library. Confirmed ingested tracks are moved here into an {Artist}/{Album}/{Track} - {Title} structure. Point this at your existing music share." Type="Path" Display="always" Required="false" Mask="false">/mnt/user/Music</Config>
  <Config Name="Library DB Directory" Target="/data/db" Default="/mnt/user/appdata/spinmatch/db" Mode="rw" Description="Storage for the SQLite library index (library.db) that powers the collection dashboard and album gap detection. Must be a persistent, mounted path so the index survives container rebuilds." Type="Path" Display="always" Required="false" Mask="false">/mnt/user/appdata/spinmatch/db</Config>
  <Config Name="MB Contact Email" Target="MB_CONTACT_EMAIL" Default="" Mode="" Description="Required: a real contact email, per the MusicBrainz API usage policy (https://musicbrainz.org/doc/MusicBrainz_API/Rate_Limiting). Every request identifies itself with this address; MusicBrainz may block the app's IP without it." Type="Variable" Display="always" Required="true" Mask="false"></Config>
  <Config Name="MB App Name" Target="MB_APP_NAME" Default="Spinmatch" Mode="" Description="Application name sent in the MusicBrainz User-Agent header." Type="Variable" Display="advanced" Required="false" Mask="false">Spinmatch</Config>
  <Config Name="MB App Version" Target="MB_APP_VERSION" Default="0.1.0" Mode="" Description="Application version sent in the MusicBrainz User-Agent header." Type="Variable" Display="advanced" Required="false" Mask="false">0.1.0</Config>
  <Config Name="AcoustID API Key" Target="ACOUSTID_API_KEY" Default="" Mode="" Description="Optional: enables automatic track identification for the local library ingest feature (which itself just needs Ingest Directory and Music Directory above). Get a free key at https://acoustid.org/new-application. Leave blank and ingest still works, falling back to matching files by their existing tags (confirmed against MusicBrainz by title and duration), with anything unconfirmed listed for manual review." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="Library DB Path" Target="LIBRARY_DB" Default="/data/db/library.db" Mode="" Description="Path (inside the container) to the SQLite library index. Should point into Library DB Directory above so it persists across container rebuilds. The collection dashboard is enabled automatically whenever Music Directory is set." Type="Variable" Display="advanced" Required="false" Mask="false">/data/db/library.db</Config>
  <Config Name="Ingest Dir Path" Target="INGEST_DIR" Default="/data/ingest" Mode="" Description="Path (inside the container) to the ingest drop-folder. Must match the container side of Ingest Directory above — the path mapping alone doesn't tell the app where to look, this variable does. Clearing it disables the Ingest page." Type="Variable" Display="advanced" Required="false" Mask="false">/data/ingest</Config>
  <Config Name="Music Dir Path" Target="MUSIC_DIR" Default="/data/music" Mode="" Description="Path (inside the container) to the organized music library. Must match the container side of Music Directory above — the path mapping alone doesn't tell the app where to look, this variable does. Clearing it disables both the Ingest and Library pages." Type="Variable" Display="advanced" Required="false" Mask="false">/data/music</Config>
  <Config Name="Trust Proxy" Target="TRUST_PROXY" Default="" Mode="" Description="Set to 1 when Spinmatch sits behind a reverse proxy you control (SWAG, Nginx Proxy Manager, Traefik, Cloudflare Tunnel). It makes the app trust X-Forwarded-For, so the login rate limit sees real client IPs instead of lumping everyone into one bucket, and X-Forwarded-Proto, so the session cookie is marked Secure when your users are on HTTPS even though the proxy talks plain HTTP to the container. Leave blank if you reach the container directly by IP and port &#8212; when it is set, the app trusts headers that any client can send." Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
  <Config Name="YTDLP Path" Target="YTDLP_PATH" Default="yt-dlp" Mode="" Description="Path/name of the yt-dlp binary. The image installs yt-dlp on PATH already; only change this if you've customized the container." Type="Variable" Display="advanced" Required="false" Mask="false">yt-dlp</Config>
  <Config Name="FPCALC Path" Target="FPCALC_PATH" Default="fpcalc" Mode="" Description="Path/name of the fpcalc (Chromaprint) binary used for acoustic fingerprinting. The image installs it on PATH already; only change this if you've customized the container." Type="Variable" Display="advanced" Required="false" Mask="false">fpcalc</Config>
</Container>
